{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,subprocess; d=json.load(sys.stdin); cmd=d.get('tool_input',{}).get('command',''); sys.exit(0) if 'git push' not in cmd else sys.exit(subprocess.call(['bash','scripts/validate.sh']))\""
          }
        ]
      }
    ]
  }
}
